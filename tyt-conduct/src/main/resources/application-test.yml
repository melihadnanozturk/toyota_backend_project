server:
  port: 8081

spring:
  application:
    name: tyt-conduct
  datasource:
    url: jdbc:postgresql://host.docker.internal:5432/postgres
    username: postgres
    password: root123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

tyt:
  32Bit:
    feign-client:
      auth: http://host.docker.internal:8080