package com.mao.tytmistake.controller.request.page;

/**
 * Interface representing a sortCol in the TYT (Toyota-Backend) system.
 */
public interface TytSortCol {

    /**
     * Returns the sort column value with a colon.
     *
     * @return the sort column value with a colon
     */
    String getColon();
}
